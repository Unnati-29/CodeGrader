{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "pip install -r requirements.txt && python manage.py collectstatic --noinput"
  },
  "deploy": {
    "preDeployCommand": "python manage.py migrate && python manage.py createsu",
    "startCommand": "gunicorn codegrader.wsgi --log-file -"
  }
}